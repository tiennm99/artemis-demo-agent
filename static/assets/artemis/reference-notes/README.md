# Artemis Reference Assets

This folder documents the SvelteKit static asset boundary.

The current demo assets are copied into `static/assets/artemis/reference/` during scaffold setup so the first SvelteKit shell can preserve the Artemis visual direction. Production-owned recreated assets should later live under `static/assets/artemis/generated/`.

Do not treat copied reference assets as final production art until ownership/licensing is accepted.
